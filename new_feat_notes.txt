python script that handles website enumeration data:
	-organize and create directories
		make an additional file with all subdomains
		status_code
	-pick tech fingerprint from js enumeration
	-integrate with networking recon automation:
		asn
		ips
			open ports
		status_code

workflow frameworks to do:
	permutation
	dns resolution
bash script for full networking recon

tasks orchestration (Luigi, Prefect, Airflow) -> load distribution across multiple servers (Kubernets, fleet, Axiom)
