google dorking params:
	site: -> tells google what site the content of the search is
    inurl: -> directory paths "/example/login.php"
    intile: -> note that file-listening pages are often have the title "index of"
    link: -> searches for web pages that contains this specific link
    filetype: -> specify the extension of files you want
    wildcard(\*) -> separates the search in 2 periods, like a sql query: "find x WHERE y is in the end"
    quotes(" "): -> forces exactly matches
    or(|): -> search 2 terms or more
    minus(-): -> excludes certain results

google hacking database: https://www.exploit-db.com/google-hacking-database/
