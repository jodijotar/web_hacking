#checkpoint

map application features from all_urls.txt and api_endpoints.txt: auth flows, user roles, file uploads, redirects, payment logic

define threat model:

	what is the worst that could happen to this org?

builds and mindmap with all the data collected so far that can picture the threat model

next phase -> [[6 - targeted vulnerability assessment]]