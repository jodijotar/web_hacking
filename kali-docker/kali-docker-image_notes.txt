apt update && apt -y install kali-linux-headless -> the metapackage that does not require GUI

to resume an exited container session:
    docker container list --all / docker ps -a
    docker start d36922fa21e8
    docker attach d36922fa21e8

to remove a container session:
    docker rm d36922fa21e8

