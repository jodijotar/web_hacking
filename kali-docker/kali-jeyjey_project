2--
make a Dockerfile that automatically install all the tools
    have the kali headless + install the tools i need

make a directory host volume reference in the Dockerfile (recon-workspace)
    bind mouts:
        docker run --mount type=bind,src="$(pwd)/kali-workspace",dst=/kali-workspace
        (the directory kali-workspace must already exist in the host machine)

    --- docker run --mount type=bind,src="/home/jeyjey/kali-workspace",dst=/kali-workspace --tty --interactive --name kali-jeyjey kalilinux/kali-rolling ---

3--
deploy my custom kali-jeyjey image
commit the Dockerfile and related files in web_hacking


first use:
    building from image - kali-rolling:
        --- 
docker run --mount type=bind,src="/home/jeyjey/kali-workspace",dst=/kali-workspace --tty --interactive --name kali-jeyjey kalilinux/kali-rolling 
        ---
    apt update && apt -y install kali-linux-headless
    




